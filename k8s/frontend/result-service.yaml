apiVersion: v1
kind: Service
metadata:
  name: result
  namespace: voting-app
  labels:
    app: result
spec:
  selector:
    app: result
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: 31001